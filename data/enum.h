#ifndef ENUM_H
#define ENUM_H

enum Direction
{
    UP,
    DOWN
};

enum BusyScheduling
{
    SHORTEST_SEEK_TIME_FIRST,
    LINEAR_SCAN
};

enum IdleScheduling
{
    TOP,
    BOTTOM
};

#endif // ENUM_H
